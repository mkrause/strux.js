
/*
import chai, { assert, expect } from 'chai';

import BaseText from '../structures/Text.js';
import BaseNumber from '../structures/Number.js';

import Mapping from '../structures/Mapping.js';


describe('Mapping', function() {
    describe('constructor', function() {
        it('should construct a Mapping from a native Map', function() {
            expect(() => {
                const entries = new Map([
                    [new BaseText('foo'), new BaseNumber(42)],
                    [new BaseText('bar'), new BaseNumber(43)],
                ]);
                const mapping = new Mapping(entries);
            }).to.not.throw();
        });
    });
});
*/
